import { StyledDirective } from './styled.directive';

describe('StyledDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledDirective();
    expect(directive).toBeTruthy();
  });
});
